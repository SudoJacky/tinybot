# Native Browser Test Support

This directory contains fixtures shared by native browser tests. The fixtures
provide deterministic browser state and local pages without becoming part of
the production browser runtime.
